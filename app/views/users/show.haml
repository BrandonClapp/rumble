.main-banner
  .main-banner-film

.container
  .content

    %h2= @user.name
    = @user.email
    = @user.role

    - if policy(@user).update?
      .dark
        %p= link_to "Edit Account", edit_user_registration_path